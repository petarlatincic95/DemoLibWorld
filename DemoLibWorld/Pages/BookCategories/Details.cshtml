@page
@model DemoLibWorld.Pages.BookCategories.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>BookCategory</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.BookCategory.CategoryName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.BookCategory.CategoryName)
        </dd>
    </dl>
</div>
<br/>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.BookCategory?.Id" class="btn btn-primary">Edit</a> 
    <a asp-page="./Index" class="btn btn-primary">Back</a>
</div>
